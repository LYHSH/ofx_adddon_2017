#include "ofxCannonComPJcontrol.h"



ofxCannonComPJcontrol::ofxCannonComPJcontrol()
{
}


ofxCannonComPJcontrol::~ofxCannonComPJcontrol()
{
}

void ofxCannonComPJcontrol::setOnOoff(bool _state)
{
	/*static int const nbaud = 9600;
	string hexCmd = "";

	if (_state)
	{
		ofLogNotice(OF_FUNCTION_NAME) << "COM  " + getCOM() + "          on" << endl;
		hexCmd = "7E504E0D";
	}
	else
	{
		ofLogNotice(OF_FUNCTION_NAME) << "COM  " + getCOM() + "          off" << endl;
		hexCmd = "7E50460D";
	}
	string command = ofHexToString(hexCmd);

	sendCmd(command, nbaud);*/
}
